<%= turbo_stream.update(:notifications) { render(ToastrComponent.new(flash: flash)) } %>
<%= turbo_stream.update 'modal', nil %>

<% if @videos.present? %>
  <%= turbo_stream.update 'video_list' do %>
    <%= render VideoComponent.with_collection(@videos) %>
  <% end %>
<% end %>
